language: python

script:
  - flake8
  - yamllint drift/openapi/api.spec.yaml
  - nosetests --with-coverage --cover-package drift  --cover-min-percentage 95 --cover-erase

install:
  - pip install -r requirements.txt
  - pip install -r test-requirements.txt
